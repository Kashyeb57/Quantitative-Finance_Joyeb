---
title: "Power Law Distribution"
sidebar_position: 1
description: The 80–20 rule made precise — a functional relationship where one quantity varies as a power of another, producing a long tail where a few outcomes dominate. Log-log straight lines, the transform back to normal, and why finance lives here.
---

# Power Law Distribution

**A power law is a functional relationship between two quantities where a relative change in one produces a *proportional* relative change in the other — independent of the initial size of those quantities.** In one line: **one quantity varies as a *power* of another**,

$$
y \;\propto\; x^{-\alpha}
$$

That "independent of the initial size" clause is the deep part: it means the law looks the **same at every scale** (double $x$ and $y$ changes by the same factor whether you started small or large). This *scale invariance* is what produces the signature shape — a few outcomes that utterly dominate, and a **long tail** of many small ones.

## The 80–20 rule and the long tail

The everyday face of a power law is the **80–20 rule**: roughly 80% of the effect comes from 20% of the causes. A handful of items on the left **dominate**; the rest trail off into a long, thin **tail** on the right.

<div style={{textAlign: 'center', margin: '1.25rem 0'}}>
  <svg viewBox="0 0 460 210" width="460" style={{maxWidth: '100%'}} role="img" aria-label="A power-law curve: a few tall bars on the left (the vital few) and a long thin tail to the right">
    <line x1="40" y1="175" x2="440" y2="175" stroke="currentColor" strokeWidth="1" opacity="0.5" />
    <line x1="40" y1="175" x2="40" y2="20" stroke="currentColor" strokeWidth="1" opacity="0.5" />
    <rect x="46" y="35" width="18" height="140" fill="#25c2a0" opacity="0.85" />
    <rect x="68" y="70" width="18" height="105" fill="#25c2a0" opacity="0.8" />
    <rect x="90" y="100" width="18" height="75" fill="#25c2a0" opacity="0.75" />
    <rect x="112" y="122" width="18" height="53" fill="#0e7490" opacity="0.6" />
    <rect x="134" y="137" width="18" height="38" fill="#0e7490" opacity="0.55" />
    <rect x="156" y="147" width="18" height="28" fill="#0e7490" opacity="0.5" />
    <rect x="178" y="154" width="18" height="21" fill="#0e7490" opacity="0.45" />
    <rect x="200" y="159" width="18" height="16" fill="#0e7490" opacity="0.4" />
    <rect x="222" y="162" width="18" height="13" fill="#0e7490" opacity="0.38" />
    <rect x="244" y="164" width="18" height="11" fill="#0e7490" opacity="0.35" />
    <rect x="266" y="166" width="18" height="9" fill="#0e7490" opacity="0.32" />
    <rect x="288" y="167" width="18" height="8" fill="#0e7490" opacity="0.3" />
    <rect x="310" y="168" width="18" height="7" fill="#0e7490" opacity="0.28" />
    <rect x="332" y="169" width="18" height="6" fill="#0e7490" opacity="0.26" />
    <rect x="354" y="170" width="18" height="5" fill="#0e7490" opacity="0.24" />
    <text x="100" y="30" textAnchor="middle" fontSize="11" fill="#25c2a0" fontWeight="700">the vital few (≈20%)</text>
    <text x="330" y="150" textAnchor="middle" fontSize="11" fill="currentColor" opacity="0.7">the long tail (≈80% of items, little each)</text>
    <text x="240" y="200" textAnchor="middle" fontSize="11" fill="currentColor" opacity="0.7">rank / value →</text>
  </svg>
</div>

The lecture's examples span sports, economics, and engineering:

1. **IPL / sports:** ~20% of the team is responsible for winning ~80% of the matches.
2. **Wealth:** ~80% of the wealth is held by ~20% of the population.
3. **Oil:** ~80% of the total oil is held by ~20% of the nations.
4. **Language:** the frequencies of words in most languages (a few words like "the" dominate — Zipf's law).
5. **Software:** fixing ~20% of the major defects removes ~80% of the upcoming defects in a product.

## The tell-tale sign: straight on log-log axes

A power law $y = c\,x^{-\alpha}$ becomes a **straight line** when you plot $\ln y$ against $\ln x$ (slope $-\alpha$). That's the practical test: histogram your data on log-log axes, and if it's a straight line, it's a power law — not a bell.

```python
import numpy as np
rng = np.random.default_rng(0)

# a power-law sample (Pareto): check the 80-20 concentration
x = rng.pareto(a=1.16, size=100_000) + 1     # a≈1.16 gives roughly 80-20
x.sort()
top20_share = x[int(0.8*len(x)):].sum() / x.sum()
print(f"share of total held by the top 20%: {top20_share*100:.0f}%")   # ~80%

# power law => straight line on log-log axes
vals, edges = np.histogram(x, bins=np.logspace(0, 2, 30), density=True)
centers = np.sqrt(edges[:-1]*edges[1:])
mask = vals > 0
slope = np.polyfit(np.log(centers[mask]), np.log(vals[mask]), 1)[0]
print(f"slope on log-log axes ≈ {slope:.2f}  (a straight line => power law)")
```

## Taming a power law: transform back to normal

Power-law data breaks every normal-based tool (its mean and variance can be unstable or even infinite). The fix is a **transformation** that pulls the long tail back in:

$$
\text{Power law} \;\xrightarrow{\;\ln(x)\;}\; \text{closer to Normal}, \qquad \text{Power law} \;\xrightarrow{\text{Box–Cox}}\; \text{Normal} \;\Rightarrow\; \text{QQ plot}
$$

Taking logs (or the more general **Box–Cox transform**) compresses the tail so a [QQ plot](/docs/Probability/distributions/continuous/normal) straightens out — after which normal methods can be applied to the transformed data. The named probability distribution that formalizes the power law is the **[Pareto distribution](/docs/Probability/distributions/heavy-tails/pareto)** (the "80–20" distribution), the next lesson.

## Why a quant cares

Finance is a **power-law world wearing a normal-shaped costume**:

- **Market cap, city sizes, firm sizes, trading volume** are power-law distributed — a few mega-caps dominate an index, exactly as a few words dominate a language.
- **The size of market moves has power-law tails.** Crash magnitudes don't decay like a thin normal tail; they follow a power law, so "impossible" 10-sigma days keep happening. This is the heart of Mandelbrot's and Taleb's critique of Gaussian finance — the **black swan** lives in the power-law tail.
- **Concentration risk.** If 20% of your positions drive 80% of your P&L (or your risk), diversification is an illusion — the power law says the tail *is* the portfolio.

Assuming normality where a power law rules is the single most expensive mistake in quantitative risk. Recognizing the straight line on log-log axes is how you catch it.

## Check your understanding

<Quiz questions={[
  {
    q: 'The "80–20 rule" is an everyday description of…',
    options: ['a normal distribution', 'a power-law distribution (a few causes dominate the effect)', 'a uniform distribution', 'the empirical rule'],
    answer: 1,
    explain: 'Power laws produce concentration: a small fraction of items accounts for most of the total — the 80–20 (Pareto) pattern.',
  },
  {
    q: 'A power law y ∝ x^(−α) plotted with ln(y) against ln(x) looks like…',
    options: ['a bell curve', 'a straight line with slope −α', 'a flat line', 'an S-curve'],
    answer: 1,
    explain: 'Taking logs of a power law gives a linear relationship: log-log axes turn it into a straight line of slope −α — the standard diagnostic.',
  },
  {
    q: 'Why is assuming normality dangerous when returns actually follow a power law?',
    options: [
      'normal tails are too fat',
      'power-law tails are much heavier, so extreme moves (crashes) are far more likely than a normal predicts',
      'power laws have no mean ever',
      'they are the same distribution',
    ],
    answer: 1,
    explain: 'Power-law tails decay slowly, making large events far more probable than the thin normal tail implies — so Gaussian risk models drastically understate crash risk.',
  },
]} />
