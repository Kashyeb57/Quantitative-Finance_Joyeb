---
title: "Chapter 15: Multiple Integrals"
sidebar_position: 15
---

import HandPlot from '@site/src/components/CalcLab/HandPlot';
import CalcPlot from '@site/src/components/CalcPlot';

### 15.1–15.2 Double Integrals

$$
\iint_R f(x,y) \, dA = \text{volume under surface } z=f(x,y) \text{ over region } R
$$

**FUBINI'S THEOREM** (iterate the integral):
$$
\iint_R f(x,y) \, dA = \int_a^b \int_c^d f(x,y) \, dy \, dx = \int_c^d \int_a^b f(x,y) \, dx \, dy
$$

> Evaluate inner integral first, then outer.

<CalcPlot name="double-integral" />


---

### 15.3 Polar Coordinates for Double Integrals

**Area element in polar:** $dA = r \, dr \, d\theta$

$$
\iint_R f(x,y) \, dA = \iint f(r\cos\theta, r\sin\theta) \, r \, dr \, d\theta
$$

> **Why $r$?** Consider an annular ring at radius $r$. The circumference is $2\pi r$, and thickness is $dr$. The area of this tiny ring is $(2\pi r)(dr)$, meaning the area element scales with $r$.

<HandPlot
  title="Polar Annulus"
  xLabel="x" yLabel="y" domain={[-3, 3]} range={[-3, 3]}
  polygons={[
    { points: [[1,0], [1.4,1.4], [0,2], [-1.4,1.4], [-2,0], [-1.4,-1.4], [0,-2], [1.4,-1.4], [1,0]], color: 'green' },
    { points: [[0.5,0], [0.7,0.7], [0,1], [-0.7,0.7], [-1,0], [-0.7,-0.7], [0,-1], [0.7,-0.7], [0.5,0]], color: 'white' }
  ]}
  caption="An annulus region defined by inner radius a and outer radius b. Useful for circles, rings, and sectors!"
  height={300}
/>

---

### 15.6–15.8 Triple Integrals and Coordinate Systems

Triple integral over a volume $E$:
$$
\iiint_E f(x,y,z) \, dV
$$

**CYLINDRICAL $(r, \theta, z)$:**
$$
x = r \cos\theta, \quad y = r \sin\theta, \quad z = z
$$
$$
dV = r \, dz \, dr \, d\theta
$$
*(Useful for cylinders, cones, and rotationally symmetric objects around the z-axis).*

**SPHERICAL $(\rho, \phi, \theta)$:**
$$
x = \rho \sin\phi \cos\theta
$$
$$
y = \rho \sin\phi \sin\theta
$$
$$
z = \rho \cos\phi
$$
$$
dV = \rho^2 \sin\phi \, d\rho \, d\phi \, d\theta
$$

- $\rho = \text{distance from origin}$
- $\phi = \text{polar angle from positive z-axis}$
- $\theta = \text{azimuthal angle (same as polar } \theta \text{)}$
*(Use spherical coordinates for spheres and balls).*

<CalcPlot name="coordinate-systems" />


---

### 15.9 Change of Variables (Jacobian)

$$
\iint_R f(x,y) \, dA = \iint_S f(x(u,v), y(u,v)) \cdot |J| \, du \, dv
$$

**Jacobian Determinant:**
$$
J = \frac{\partial(x,y)}{\partial(u,v)} = \begin{vmatrix} \frac{\partial x}{\partial u} & \frac{\partial x}{\partial v} \\ \frac{\partial y}{\partial u} & \frac{\partial y}{\partial v} \end{vmatrix}
$$

> $|J|$ is the "stretch factor" of the transformation.

**Example: Polar coordinates**
$x = r \cos\theta, \quad y = r \sin\theta$
$J = r \implies dA = r \, dr \, d\theta$
