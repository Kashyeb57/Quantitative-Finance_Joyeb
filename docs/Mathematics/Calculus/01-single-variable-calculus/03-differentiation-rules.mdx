---
title: "Chapter 3: Differentiation Rules"
sidebar_position: 3
---

import HandPlot from '@site/src/components/CalcLab/HandPlot';

### 3.1 Basic Differentiation Rules

```
  ┌────────────────────────────────────────────┐
  │  RULE              FORMULA                 │
  ├────────────────────────────────────────────┤
  │  Constant          d/dx[c] = 0             │
  │  Power             d/dx[xⁿ] = nxⁿ⁻¹       │
  │  Constant Multiple d/dx[cf] = c·f'         │
  │  Sum               d/dx[f+g] = f'+g'       │
  │  Difference        d/dx[f−g] = f'−g'       │
  │  Exponential       d/dx[eˣ] = eˣ           │
  │  General Exp       d/dx[bˣ] = bˣ·ln(b)     │
  └────────────────────────────────────────────┘
```

**Visual: Power Rule**

<HandPlot
  title="Power Rule: f(x) = x³ and f'(x) = 3x²"
  xLabel="x" yLabel="y"
  domain={[-2, 2]}
  range={[-6, 8]}
  curves={[
    { f: (x) => x*x*x, color: 'ink', label: 'f(x) = x³ (cubic)' },
    { f: (x) => 3*x*x, color: 'blue', dash: true, label: "f'(x) = 3x² (≥ 0)" },
  ]}
/>

---

### 3.2 Product and Quotient Rules

**Product Rule:**
```
  d/dx[f·g] = f'·g + f·g'

  Think: "first times derivative of second
         plus second times derivative of first"

  Example: d/dx[x²·sin(x)]
         = 2x·sin(x) + x²·cos(x)
```

**Quotient Rule:**
```
  d/dx[f/g] = (f'·g − f·g') / g²

  Memory trick: "low d-high minus high d-low, over low squared"

  Example: d/dx[sin(x)/x]
         = (cos(x)·x − sin(x)·1) / x²
         = (x·cos(x) − sin(x)) / x²
```

---

### 3.3 Derivatives of Trigonometric Functions

```
  ┌───────────────────────────────────────┐
  │  d/dx[sin(x)]  =  cos(x)             │
  │  d/dx[cos(x)]  = −sin(x)             │
  │  d/dx[tan(x)]  =  sec²(x)            │
  │  d/dx[cot(x)]  = −csc²(x)            │
  │  d/dx[sec(x)]  =  sec(x)tan(x)       │
  │  d/dx[csc(x)]  = −csc(x)cot(x)       │
  └───────────────────────────────────────┘
```

**Visual: sin and its derivative cos**

<HandPlot
  title="sin(x) and its derivative cos(x)"
  xLabel="x" yLabel="y"
  domain={[0, 6.283185307]}
  range={[-1.4, 1.4]}
  curves={[
    { f: (x) => Math.sin(x), color: 'ink', label: 'sin(x)' },
    { f: (x) => Math.cos(x), color: 'blue', dash: true, label: "cos(x) = [sin(x)]'" },
  ]}
/>

- Where sin **rises** → cos is positive (slope is positive)
- Where sin **falls** → cos is negative (slope is negative)
- Where sin **peaks** → cos $= 0$ (slope $= 0$)

---

### 3.4 The Chain Rule

**The most important rule in calculus.**

```
  If y = f(u) and u = g(x), then:
  dy/dx = dy/du · du/dx = f'(g(x)) · g'(x)

  VISUAL — think of it as gears:

  x ──[g]──> u ──[f]──> y

  Rate of change of y w.r.t. x
    = (rate y changes per u) × (rate u changes per x)
```

**Examples:**
```
  d/dx[sin(x²)]  = cos(x²) · 2x
                    ↑outer      ↑inner derivative

  d/dx[e^(3x)]   = e^(3x) · 3

  d/dx[(x²+1)⁵]  = 5(x²+1)⁴ · 2x
```

**Nested chain rule:**
```
  d/dx[sin(e^(x²))] = cos(e^(x²)) · e^(x²) · 2x
                        ↑outer      ↑middle    ↑inner
```

> **Quant Finance:** Chain rule is THE fundamental tool in derivatives pricing. Itô's lemma (stochastic chain rule) derives Black-Scholes PDE.

---

### 3.5 Implicit Differentiation

Sometimes y is not solved explicitly for x. Differentiate both sides and treat y as y(x):

**Example:** $x^2 + y^2 = 25$ (circle of radius 5)

<HandPlot
  title="Implicit curve: x² + y² = 25"
  xLabel="x" yLabel="y"
  domain={[-5, 5]}
  range={[-5.5, 5.5]}
  curves={[
    { f: (x) => Math.sqrt(25 - x*x), color: 'ink', label: 'upper: y = +√(25−x²)' },
    { f: (x) => -Math.sqrt(25 - x*x), color: 'ink', label: 'lower: y = −√(25−x²)' },
  ]}
  points={[{ x: 3, y: 4, label: 'P (3,4), slope −3/4', color: 'red' }]}
/>

Differentiate both sides:

$$
2x + 2y\,\frac{dy}{dx} = 0 \quad\Longrightarrow\quad \frac{dy}{dx} = -\frac{x}{y}
$$

At point $(3,4)$: slope $= -\tfrac{3}{4}$.

> **Quant Finance:** Extracting implied volatility from option prices; yield curve bootstrapping.

---

### 3.6 Derivatives of Logarithmic Functions

```
  d/dx[ln(x)]      = 1/x        (x > 0)
  d/dx[ln|x|]      = 1/x        (x ≠ 0)
  d/dx[log_b(x)]   = 1/(x·ln b)
```

**Visual:** $\frac{d}{dx}[\ln(x)] = \frac{1}{x}$

<HandPlot
  title="ln(x) and its derivative 1/x"
  xLabel="x" yLabel="y"
  domain={[0.05, 6]}
  range={[-2, 4]}
  curves={[
    { f: (x) => Math.log(x), color: 'ink', label: 'ln(x) — crosses at (1,0)' },
    { f: (x) => 1/x, color: 'blue', dash: true, label: "1/x = [ln(x)]'" },
  ]}
  marks={[{ x: 1, label: 'x=1' }]}
/>

**Logarithmic Differentiation** — take ln of both sides first:
```
  y = xˣ
  ln(y) = x·ln(x)
  y'/y = ln(x) + 1
  y' = xˣ(ln(x) + 1)
```

---

### 3.7–3.8 Rates of Change and Exponential Growth

**Economics Applications:**

- $C(x)$ = total cost to produce $x$ units
- $C'(x)$ = marginal cost (cost of one more unit)

<HandPlot
  title="Cost C(x) and marginal cost C'(x)"
  xLabel="x (units)" yLabel="cost"
  domain={[0, 6]}
  range={[0, 20]}
  curves={[
    { f: (x) => 0.5*x*x + x, color: 'ink', label: 'C(x) — total cost' },
    { f: (x) => x + 1, color: 'blue', dash: true, label: "C'(x) — marginal cost" },
  ]}
/>

**Natural Growth/Decay:**
```
  dy/dt = ky     →     y(t) = y₀·e^(kt)

  This ODE says: "rate of change is proportional to current value"
  — bacteria, compound interest, radioactive decay, ...
```

---

### 3.9 Related Rates

Variables that all change with time — use chain rule w.r.t. t:

```
  Classic: Ladder sliding down wall

  y
  |\ ← ladder of length L
  | \
  | h\      h² + x² = L²
  |   \
  |    \
  +─x───\──> x

  Differentiate w.r.t. t:
  2h(dh/dt) + 2x(dx/dt) = 0
  → find dh/dt given dx/dt
```

---

### 3.10 Linearization and Differentials

**Linear approximation (tangent line approximation):**

$$
L(x) = f(a) + f'(a)(x - a)
$$

<HandPlot
  title="Linearization: L(x) tangent to f(x) at x = a"
  xLabel="x" yLabel="y"
  domain={[0, 4]}
  range={[0, 8]}
  curves={[
    { f: (x) => x*x*0.5, color: 'ink', label: 'curve f(x)' },
    { f: (x) => 2*(x - 2) + 2, color: 'blue', dash: true, label: 'linear approx L(x)' },
  ]}
  points={[{ x: 2, y: 2, label: '(a, f(a))', color: 'red' }]}
  marks={[{ x: 2, label: 'a' }]}
/>

$L(x) \approx f(x)$ near $x = a$.

**Differential:**
```
  dy = f'(x) dx

  When x changes by dx, y changes by approximately dy.
```

---

### 3.11 Hyperbolic Functions

$$
\sinh(x) = \frac{e^{x} - e^{-x}}{2}, \qquad
\cosh(x) = \frac{e^{x} + e^{-x}}{2}, \qquad
\tanh(x) = \frac{\sinh(x)}{\cosh(x)}
$$

<HandPlot
  title="cosh(x) — the catenary shape"
  xLabel="x" yLabel="y"
  domain={[-3, 3]}
  range={[0, 10]}
  curves={[
    { f: (x) => Math.cosh(x), color: 'ink', label: 'cosh(x)' },
  ]}
/>

<HandPlot
  title="tanh(x) — bounded between −1 and 1"
  xLabel="x" yLabel="y"
  domain={[-4, 4]}
  range={[-1.4, 1.4]}
  curves={[
    { f: (x) => Math.tanh(x), color: 'blue', label: 'tanh(x)' },
  ]}
  marks={[{ y: 1, label: 'y = 1' }, { y: -1, label: 'y = −1' }]}
/>

**Identity:** $\cosh^2(x) - \sinh^2(x) = 1$ (analogous to $\cos^2 + \sin^2 = 1$ for trig).
