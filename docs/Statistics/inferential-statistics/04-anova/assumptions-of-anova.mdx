---
title: "Assumptions of ANOVA"
sidebar_position: 2
description: The four assumptions your data must satisfy before an ANOVA is valid — normality of the sampling distribution of the mean, absence of outliers, homogeneity of variance (σ₁²=σ₂²=σ₃²), and independent, random samples.
---

# Assumptions of ANOVA

Every test carries assumptions: the [z](/docs/Statistics/inferential-statistics/tests-for-a-mean/z-test)- and t-tests lean on a normal sampling distribution, the [chi-square](/docs/Statistics/inferential-statistics/categorical-tests/goodness-of-fit) test on a right-skewed one. **[ANOVA](/docs/Statistics/inferential-statistics/anova/anova-test) has four.** If your problem satisfies them, you can go ahead and use ANOVA — and these are a favourite **interview question**, so they're worth knowing cold.

## ① Normality of the sampling distribution of the mean

**The distribution of the sample mean is normally distributed** (i.e. it follows a Gaussian). The instant you hear "sampling distribution of the mean" and "normality," the [**Central Limit Theorem**](/docs/Probability/distributions/sampling-and-estimation/central-limit-theorem) should come to mind — that's exactly what guarantees this, and it's why ANOVA is fairly forgiving here for reasonable sample sizes.

## ② Absence of outliers

**Outlying scores need to be removed from the dataset.** Even a single extreme point can distort group means and variances enough to change the verdict, so you screen for outliers first — with a [box plot, the interquartile range (IQR)](/docs/Statistics/descriptive-statistics/shape-and-position/five-number-summary), and other techniques from feature engineering.

<div style={{textAlign: 'center', margin: '1.25rem 0'}}>
  <svg viewBox="0 0 460 90" width="460" style={{maxWidth: '100%'}} role="img" aria-label="A strip of clustered data points with one far-off outlier flagged for removal">
    <line x1="30" y1="55" x2="430" y2="55" stroke="currentColor" strokeWidth="1" opacity="0.5" />
    {[70, 84, 92, 99, 105, 110, 116, 121, 128, 137].map((x, i) => (
      <circle key={i} cx={x} cy="55" r="5" fill="#25c2a0" opacity="0.85" />
    ))}
    <circle cx="400" cy="55" r="6" fill="none" stroke="#e34948" strokeWidth="2" />
    <circle cx="400" cy="55" r="5" fill="#e34948" opacity="0.85" />
    <text x="400" y="38" textAnchor="middle" fontSize="11" fill="#e34948" fontWeight="700">outlier → remove</text>
    <text x="103" y="80" textAnchor="middle" fontSize="10.5" fill="currentColor" opacity="0.7">the bulk of the data</text>
  </svg>
</div>

## ③ Homogeneity of variance

**The population variance in the different levels of each independent variable (factor) must be equal** — "homogeneity" means *homogeneous / the same*. For three levels that's:

$$
\sigma_1^2 = \sigma_2^2 = \sigma_3^2
$$

Each group can have a *different mean* (that's what ANOVA is testing), but they must share roughly the **same spread**. Left below satisfies it; right violates it (one group is far wider):

<div style={{textAlign: 'center', margin: '1.25rem 0'}}>
  <svg viewBox="0 0 620 190" width="620" style={{maxWidth: '100%'}} role="img" aria-label="Left panel: three group bells of equal width (homogeneous variance). Right panel: three bells of unequal width (violation).">
    {/* left: homogeneous */}
    <text x="150" y="16" textAnchor="middle" fontSize="12" fill="#25c2a0" fontWeight="700">✓ equal variances</text>
    <line x1="20" y1="150" x2="290" y2="150" stroke="currentColor" strokeWidth="1" opacity="0.5" />
    {[['#25c2a0', 70], ['#0e7490', 150], ['#f59e0b', 230]].map(([c, cx]) => (
      <path key={cx} d={`M${cx-42},150 C${cx-22},150 ${cx-18},62 ${cx},62 C${cx+18},62 ${cx+22},150 ${cx+42},150`} fill={c} fillOpacity="0.12" stroke={c} strokeWidth="2" />
    ))}
    <text x="150" y="172" textAnchor="middle" fontSize="10" fill="currentColor" opacity="0.7">σ₁² = σ₂² = σ₃² — same spread</text>
    {/* right: heteroscedastic */}
    <text x="470" y="16" textAnchor="middle" fontSize="12" fill="#e34948" fontWeight="700">✗ unequal variances</text>
    <line x1="330" y1="150" x2="600" y2="150" stroke="currentColor" strokeWidth="1" opacity="0.5" />
    <path d="M360,150 C372,150 374,92 384,92 C394,92 396,150 408,150" fill="#25c2a0" fillOpacity="0.12" stroke="#25c2a0" strokeWidth="2" />
    <path d="M420,150 C438,150 442,70 462,70 C482,70 486,150 504,150" fill="#0e7490" fillOpacity="0.12" stroke="#0e7490" strokeWidth="2" />
    <path d="M500,150 C530,150 538,104 566,104 C594,104 602,150 632,150" fill="#f59e0b" fillOpacity="0.12" stroke="#f59e0b" strokeWidth="2" />
    <text x="470" y="172" textAnchor="middle" fontSize="10" fill="currentColor" opacity="0.7">spreads differ — assumption broken</text>
  </svg>
</div>

("Independent variable" is just the [factor](/docs/Statistics/inferential-statistics/anova/anova-test); **independent** means one variable doesn't influence the other — more on independent vs dependent variables in the lessons ahead.)

## ④ Samples are independent and random

**The samples must be independent and randomly selected.** One observation shouldn't influence another, and each should be drawn at random from its population — the same backbone of honest sampling that every test in this module rests on.

```python
import numpy as np
from scipy import stats

rng = np.random.default_rng(0)
g1 = rng.normal(20, 3, 40)
g2 = rng.normal(22, 3, 40)
g3 = rng.normal(21, 3, 40)

# ① normality of each group (Shapiro–Wilk: p > 0.05 => can't reject normality)
for i, g in enumerate([g1, g2, g3], 1):
    print(f"group {i} normality p = {stats.shapiro(g).pvalue:.3f}")

# ② outliers via the IQR rule (flag points outside [Q1-1.5·IQR, Q3+1.5·IQR])
def iqr_outliers(x):
    q1, q3 = np.percentile(x, [25, 75]); iqr = q3 - q1
    return x[(x < q1 - 1.5*iqr) | (x > q3 + 1.5*iqr)]
print("outliers in g1:", iqr_outliers(g1))

# ③ homogeneity of variance (Levene: p > 0.05 => equal variances OK)
print(f"Levene equal-variance p = {stats.levene(g1, g2, g3).pvalue:.3f}")

# ④ independence/randomness is a design property — ensured by how you sample.
```

## Why a quant cares

The assumptions aren't box-ticking — each one, when it breaks, breaks in a way markets specialise in:

- **Homogeneity of variance is the one that fails first.** Financial groups (regimes, sectors, eras) almost never share a variance — volatility clusters and shifts. A calm-vs-crisis comparison violates $\sigma_1^2=\sigma_2^2$ badly, so a naïve ANOVA is untrustworthy; you'd reach for a **Welch's ANOVA** or a robust alternative.
- **Outliers aren't noise, they're the P&L.** "Remove the outliers" is dangerous advice in finance, where the extreme days *are* the story. Blindly deleting them to satisfy an assumption can erase exactly the risk you were trying to measure — flag and understand them, don't reflexively bin them.
- **Independence is routinely violated by time.** Return observations are autocorrelated and non-stationary, so "independent and random" rarely holds for a raw time series. That's why group-comparison tests on financial data need care (or a time-series model) rather than a textbook ANOVA.

Knowing the assumptions is step one; knowing **how they fail in markets** is what separates a real analysis from a p-value fished out of invalid conditions.

## Check your understanding

<Quiz questions={[
  {
    q: 'Which is NOT one of the four ANOVA assumptions?',
    options: [
      'normality of the sampling distribution of the mean',
      'absence of outliers',
      'the group means are all equal',
      'homogeneity of variance across groups',
    ],
    answer: 2,
    explain: 'Equal group means is the NULL HYPOTHESIS ANOVA tests — not an assumption. The four assumptions are normality, no outliers, homogeneity of variance, and independent random samples.',
  },
  {
    q: 'Homogeneity of variance means…',
    options: [
      'all group means are equal',
      'the population variances of the groups (levels) are equal: σ₁² = σ₂² = σ₃²',
      'there are no outliers',
      'the data are categorical',
    ],
    answer: 1,
    explain: 'Groups may have different means, but ANOVA assumes they share the same spread — equal variances across the levels of the factor.',
  },
  {
    q: 'The normality assumption in ANOVA is underpinned by which idea?',
    options: [
      'the chi-square distribution',
      'the Central Limit Theorem — the sampling distribution of the mean is approximately normal',
      'Bayes’ theorem',
      'the 80–20 rule',
    ],
    answer: 1,
    explain: 'ANOVA assumes the sampling distribution of the mean is normal; the CLT is what makes that hold for reasonable sample sizes.',
  },
]} />
