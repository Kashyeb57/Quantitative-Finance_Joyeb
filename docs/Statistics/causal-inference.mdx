---
sidebar_position: 8
title: Causal Inference & DAGs
description: Correlation is not causation — and directed acyclic graphs (DAGs) are the tool that tells you which variables to control for, which to leave alone, and when a regression coefficient can be read as an effect.
---

# Causal Inference & DAGs

Every quant eventually learns the hard way that **correlation is not causation** — but far fewer learn the tool that tells you *what to do about it*. A **directed acyclic graph (DAG)** is a picture of your causal assumptions: variables are nodes, and an arrow $X \to Y$ says "$X$ has a direct causal influence on $Y$." Draw the graph honestly and it tells you exactly which variables to control for to get an unbiased effect — and, just as important, which ones you must **not** touch.

> The framing here follows Richard McElreath's *Statistical Rethinking*, the source my Applied Statistical Methods course drew its DAG material from.

## Three building blocks

Every causal graph is assembled from three elementary shapes. Knowing how information flows through each is 90% of the game.

**The fork (confounder)** — $X \leftarrow Z \to Y$
A common cause $Z$ drives both $X$ and $Y$. This manufactures a correlation between $X$ and $Y$ even when neither causes the other. **You must control for $Z$** to see the true $X\to Y$ effect. This is *the* classic trap.

**The pipe (mediator)** — $X \to Z \to Y$
$X$ affects $Y$ *through* $Z$. If you control for $Z$ here, you accidentally erase the very effect you were trying to measure. **Leave mediators alone** (unless you specifically want the direct-only effect).

**The collider** — $X \to Z \leftarrow Y$
$X$ and $Y$ both cause $Z$, but are otherwise unrelated. Here the danger is the opposite: **controlling for $Z$ creates a fake correlation** between $X$ and $Y$ that wasn't there. Conditioning on a collider is a subtle, common way to fool yourself.

## Reading the graph: paths, d-separation, and the backdoor

Those three shapes combine into a rule for *any* graph. Trace every **path** connecting $X$ and $Y$ (following arrows in either direction). A path is **open** — it carries a possibly-spurious association — unless it is **blocked**, and the blocking rules come straight from the shapes:

- a **fork** or a **pipe** on the path is blocked *when you condition on* its middle variable;
- a **collider** is blocked *by default*, and conditioning on it (or on a descendant of it) *opens* it.

When every path between $X$ and $Y$ is blocked, they are **d-separated** — the graph implies no association. The recipe for an honest effect estimate is the **backdoor criterion**: control for the set of variables that blocks every *backdoor* path (a path leaving $X$ through an arrow pointing *into* $X$) without opening any collider — no more, no less. "Throw everything into the regression" fails precisely because an extra control can unblock a collider or close a pipe.

## The confounder, in code

A market example: an interest-rate regime $Z$ pushes both a bond ETF's returns ($X$) and a utility stock's returns ($Y$). Neither causes the other, yet they'll look strongly linked — until you control for the regime.

```python
import numpy as np
rng = np.random.default_rng(1)
n = 1000

Z = rng.normal(0, 1, n)                 # hidden regime (e.g. rate shock)
X = 0.8 * Z + rng.normal(0, 0.5, n)     # X is driven by Z
Y = 0.8 * Z + rng.normal(0, 0.5, n)     # Y is driven by Z — NOT by X

# 1) Naive: correlate X and Y directly
naive = np.corrcoef(X, Y)[0, 1]

# 2) Control for Z: regress Y on X *and* Z, read X's coefficient
M = np.column_stack([np.ones(n), X, Z])
b = np.linalg.lstsq(M, Y, rcond=None)[0]

print(f"naive corr(X, Y)              = {naive:+.2f}")
print(f"effect of X after adjusting Z = {b[1]:+.2f}")
```

The naive correlation looks large and convincing (~0.6); the moment you put the confounder $Z$ in the regression, $X$'s coefficient collapses to ~0 — the honest answer. **The entire skill is knowing that $Z$ needed to be there.** A DAG is how you know.

## Why this matters for markets

- **Spurious signals.** Two assets co-moving because both react to rates, the dollar, or risk-on/risk-off is confounding, not a tradable relationship. Control for the common driver before believing a pairs trade.
- **Bad controls.** Throwing "everything" into a regression is not safe — adding a mediator or a collider can *bias* the coefficient you care about. More variables ≠ more truth.
- **Backtest survivorship** is a collider bias in disguise: selecting on "still-listed" (an outcome of past returns) distorts the very relationships you're studying.

## Simpson's paradox

The most vivid confounding trap is **Simpson's paradox**: a trend that holds inside *every* subgroup can *reverse* once the groups are pooled. A strategy might show a positive exposure-vs-return slope in each volatility regime separately, yet a *negative* slope when you lump all regimes together — because the regime (a confounder) drives both exposure and returns. It is not a curiosity: it is a warning that **the answer depends on which variables you condition on**, and only a causal graph — not the data alone — tells you which grouping is the honest one. Aggregate blindly and you can conclude the exact opposite of the truth.

## Pearl's ladder of causation

Judea Pearl arranges causal questions into three rungs — and no amount of rung-1 data answers a rung-2 or rung-3 question without a causal model:

1. **Association — *seeing*.** $P(Y \mid X)$: what does *observing* $X$ tell me about $Y$? Pure correlation; this is all a plain dataset offers.
2. **Intervention — *doing*.** $P(Y \mid \operatorname{do}(X=x))$: what happens to $Y$ if I *reach in and set* $X$? The **do-operator** severs the arrows *into* $X$, cutting its confounders loose — unlike $P(Y \mid X=x)$, which merely observes $X$ happening to equal $x$ and drags all its confounders along. A correctly-adjusted regression, guided by the DAG and the backdoor criterion, estimates this interventional effect from observational data.
3. **Counterfactual — *imagining*.** $P(Y_x \mid X=x', Y=y')$: *given what actually happened*, what *would* have happened had $X$ been different? "Would my headache have cleared without the pill I took?" These are the deepest questions — retrospective, single-case, unanswerable by experiment alone — and they need a full structural model.

Most of statistics lives on rung 1. Causal inference is the discipline of climbing to rungs 2 and 3 with every assumption made explicit in the graph.

## Check your understanding

<Quiz questions={[
  {
    q: 'In a fork X ← Z → Y, to estimate the true causal effect of X on Y you should…',
    options: [
      'never control for Z',
      'control for the confounder Z',
      'control for X only',
      'condition on a collider instead',
    ],
    answer: 1,
    explain: 'Z is a common cause that induces a spurious X–Y association. Adjusting for Z removes the backdoor path and reveals the true effect (which may be zero).',
  },
  {
    q: 'In a pipe X → Z → Y, controlling for the mediator Z will…',
    options: [
      'reveal the total effect of X',
      'block part or all of the effect you were trying to measure',
      'have no consequence',
      'always create a collider',
    ],
    answer: 1,
    explain: 'Z carries X’s influence to Y. Conditioning on it removes that pathway, shrinking or erasing the very effect of interest.',
  },
  {
    q: 'Conditioning on a collider (X → Z ← Y) tends to…',
    options: [
      'remove confounding',
      'induce a spurious association between X and Y that wasn’t there',
      'estimate the causal effect perfectly',
      'do nothing',
    ],
    answer: 1,
    explain: 'Selecting on or controlling for a common effect opens a non-causal path, manufacturing correlation. It’s a classic way analyses go wrong.',
  },
  {
    q: 'Two assets are strongly correlated only because both respond to interest-rate shocks. This is an example of…',
    options: ['a genuine causal link', 'confounding by a common driver', 'a mediator effect', 'perfect hedging'],
    answer: 1,
    explain: 'The rate regime is a fork/confounder driving both. Control for it and the direct asset-to-asset “relationship” typically vanishes.',
  },
  {
    q: 'P(Y | do(X=x)) differs from P(Y | X=x) because…',
    options: [
      'they are always identical',
      'do(·) represents actively setting X (an intervention), cutting its incoming causes, while conditioning merely observes X and keeps its confounders',
      'do(·) only works for binary variables',
      'conditioning requires an experiment',
    ],
    answer: 1,
    explain: 'The do-operator severs the arrows into X, isolating X’s effect; passive conditioning lets confounders ride along, which is why observational correlations mislead.',
  },
  {
    q: 'A strategy shows a positive exposure-vs-return slope within every volatility regime, but a negative slope when all regimes are pooled. This reversal is…',
    options: ['a coding bug', "Simpson's paradox from a regime confounder", 'pure sampling noise', 'a collider that was correctly controlled'],
    answer: 1,
    explain: 'When a confounder (the regime) drives both exposure and returns, the pooled trend can flip the within-group trend. Which grouping is honest is a causal question, not a data question.',
  },
  {
    q: 'The backdoor criterion says to control for a set of variables that…',
    options: [
      'is as large as possible',
      'blocks every backdoor path into X without opening any collider',
      'always includes the mediators',
      'always contains the outcome Y',
    ],
    answer: 1,
    explain: 'Adjust for exactly the set that closes non-causal (backdoor) paths while leaving mediators and colliders alone — extra controls can bias the estimate, not sharpen it.',
  },
]} />
